apiVersion: v1
kind: Service
metadata:
  name: {{ include "cmind-api-incidents.fullname" . }}
  labels:
    {{- include "cmind-api-incidents.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cmind-api-incidents.selectorLabels" . | nindent 4 }}
