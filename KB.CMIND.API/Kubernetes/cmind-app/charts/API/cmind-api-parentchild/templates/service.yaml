apiVersion: v1
kind: Service
metadata:
  name: {{ include "cmind-api-parentchild.fullname" . }}
  labels:
    {{- include "cmind-api-parentchild.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cmind-api-parentchild.selectorLabels" . | nindent 4 }}
