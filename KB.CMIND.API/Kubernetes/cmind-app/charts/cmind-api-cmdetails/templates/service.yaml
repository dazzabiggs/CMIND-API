apiVersion: v1
kind: Service
metadata:
  name: {{ include "cmind-api-cmdetails.fullname" . }}
  labels:
    {{- include "cmind-api-cmdetails.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cmind-api-cmdetails.selectorLabels" . | nindent 4 }}
