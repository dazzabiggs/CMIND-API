apiVersion: v1
kind: Service
metadata:
  name: {{ include "cmind-api-medication.fullname" . }}
  labels:
    {{- include "cmind-api-medication.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cmind-api-medication.selectorLabels" . | nindent 4 }}
